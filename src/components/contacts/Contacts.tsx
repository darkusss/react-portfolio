import React from 'react';
import { ContactsWrapper } from './contacts.style';

export default function Contacts() {
    return (
        <ContactsWrapper>
            <h1>Contacts</h1>
        </ContactsWrapper>
    );
}